# Installation And Operation

Install globally with `python scripts/install-global-zoo-agent-kit.py --dry-run`, verify targets are user-level only, then run `python scripts/install-global-zoo-agent-kit.py`.

v3.6 first-run intake is read-only. Coding tasks require goal contract, run ledger, project profile, obligation ledger, risk classification, test strategy, quality gate, obligation coverage, review, release/operational readiness when triggered, metrics, and final evidence.

DeepSeek V4 Flash handles bounded drafting/execution/mechanical checks. GPT-5.5 handles final planning, branch decisions, semantic review, integration, curation, architecture, security, and release decisions.
