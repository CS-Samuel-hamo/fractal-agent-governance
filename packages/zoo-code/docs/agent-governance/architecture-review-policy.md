# Architecture Review Policy

ADR is required for cross-module architecture, public API changes, database schema/migration, auth/security boundary changes, new external dependency, shared type or utility creation, Proc/Processor data-source strategy change, dependency direction change, or Obligation Ledger architecture uncertainty.

DeepSeek may draft an ADR. GPT-5.5 must approve architecture decisions before integrator can merge high-risk architecture changes.
