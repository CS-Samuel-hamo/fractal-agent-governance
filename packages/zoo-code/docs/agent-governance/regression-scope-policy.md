# Regression Scope Policy

Regression scope is selected from affected callers, public interfaces, compatibility promises, data migration impact, security boundary changes, operational blast radius, and obligations in the ledger. Existence-only tests do not satisfy behavior coverage.
