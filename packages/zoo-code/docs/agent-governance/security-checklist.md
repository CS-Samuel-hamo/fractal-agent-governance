# Security Checklist

Do not read or print secrets. Check auth, authorization, PII/user data, input validation, SQL/NoSQL injection, SSRF, XSS, path traversal, unsafe file operations, dependency supply chain, destructive commands, and production config. Human gate is required for auth, permissions, payment, PII, production config, migrations, secret handling, or destructive operations.
