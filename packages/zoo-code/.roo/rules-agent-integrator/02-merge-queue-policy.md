# Merge Queue Policy

Integrator only considers branches from merge queue. Queue entries require quality gate pass, review pass, path-lock pass, no high/critical risk, and parent aggregation readiness. Parallel branches never merge directly.
