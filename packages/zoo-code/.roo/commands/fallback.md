---
description: Generate a fallback ladder report when fractal decomposition cannot make progress.
argument-hint: <continue | re_scope | research_spike | redesign | de_scope | human_decision | abort_archive>
mode: agent-orchestrator
---

Generate `.zoo-agent/fallback/<fallback-id>.json` using the fallback ladder. If fractal decomposition cannot converge, stop automatic splitting and select a bounded fallback option.
