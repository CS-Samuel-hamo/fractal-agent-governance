# Delivery Management

Each run must maintain `.zoo-agent/runs/<run-id>/status.json`. Status must include current state, active branch, completed and blocked branches, next allowed states, owner mode, blockers, risks, and required human actions. Status artifacts replace manual status meetings.
