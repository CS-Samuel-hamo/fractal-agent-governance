# Implementation Delivery Kernel

For coding tasks, planning must terminate into executable implementation queue
items and Codex task packs. Product documents are inputs and evidence, not the
default output.

Before parent aggregation or merge queue entry, coding branches must pass the
code delivery gate or record a precise blocker.
