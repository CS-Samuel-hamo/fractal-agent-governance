# Release Governance

`/release` never deploys. It creates a readiness report. Integrator cannot bypass release readiness when triggered. Migration/auth/payment/PII/production config releases require human gate.
