# Local Optimization Control

Local improvement loops must not block delivery. Continue optimization only when
it closes a root-goal obligation, resolves a blocker, or passes a required
quality gate. Otherwise write the idea to follow-up backlog and continue the
current run.
