# Codex Install Prompt For v3.7

Install this v3.7 Fractal + Zoo Native Runtime pack globally. First run validate, then dry-run, confirm targets are only `~/.roo`, Zoo/Roo global `custom_modes.yaml`, VS Code/Cursor local extension dirs, and backup dirs, then run formal install.

Do not copy the kit into business repos. Do not read or write secrets, API keys, tokens, `.env`, credentials, or provider profiles.
