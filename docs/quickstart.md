# Quickstart

1. Review the repository safety policy.
2. Inspect `packages/zoo-code/`.
3. Run `python scripts/validate-open-source-package.py`.
4. Run the Zoo Code package validator if available.
5. Install the adapter globally only after local review.
6. Reload VS Code / Zoo Code.
7. Use `/agent-run` on a toy project first.
