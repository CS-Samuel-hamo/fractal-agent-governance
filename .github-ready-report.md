# GitHub Alpha Readiness Report

Version: 0.4.0-local-alpha

public_ready: yes

## Blocking Issues

- None for local alpha source publication.
- Runtime caveat: Codex CLI backend is available, but the latest manual health probe required reconnects before returning `OK`. Treat backend latency and reconnect behavior as an alpha risk for real project trials.
- Runtime caveat: an actual temporary code task reached Codex execution, but Codex could not run local shell/file inspection inside the Windows sandbox and the scope guard blocked delivery. This is acceptable governance behavior, but it means real code delivery should start with tiny, non-critical tasks.

## Sensitive Scan Result

Result: pass.

The local scan checked candidate public files for:

- `.env` path candidates
- `.codex` path candidates
- user-specific Codex home paths
- private-key headers
- obvious API key, token, or secret assignments
- temporary smoke/alpha repo paths
- local user profile paths

No matches were found in tracked or candidate public files after the latest changes.

## Public Files Ready

- README.md
- LICENSE
- SECURITY.md
- CONTRIBUTING.md
- CHANGELOG.md
- docs/
- examples/
- scripts/
- templates/
- wrappers/
- agent launchers, including bin/agent.cmd and bin/agent.ps1

## Recommended Repo Name

`zoo-cli-agent-runtime`

## Recommended Topics

- cli
- ai-agent
- coding-agent
- codex-cli
- agent-runtime
- governance
- worktree
- developer-tools
- local-first
- experimental

## Files To Exclude

- `.tmp/`
- `.zoo-agent/runs/`
- `.zoo-agent/worktrees/`
- `.zoo-agent/evals/`
- `.zoo-agent/metrics/`
- `.codex/`
- `.env`
- `.env.*`
- `__pycache__/`
- `.pytest_cache/`
- `*.pyc`
- temporary smoke or alpha test repositories
- user-specific Codex homes
- real business project paths or runtime state

## Files To Review Manually

- `.roo/` compatibility commands/rules
- older long-form docs under `docs/` that describe pre-v4 role names
- wrapper scripts that may mention local runtime assumptions
- README and quickstart examples before public announcement

## Publish Checklist

- Run `python scripts/validate_starter_pack.py`.
- Run `python scripts/test_cli_runtime_paths.py`.
- Run `python scripts/test_cli_local_alpha.py`.
- Run `python scripts/test_cli_real_usage_alpha.py`.
- Run a sensitive scan over staged files before pushing.
- Confirm no `.codex`, `.env`, private keys, API keys, tokens, local Codex home, or temporary test repos are staged.
- Confirm examples contain only synthetic demo content.
- Confirm docs describe alpha/experimental status and do not claim production readiness.
- Confirm no automatic GitHub upload, remote creation, push, merge, deploy, or release step has been performed.
