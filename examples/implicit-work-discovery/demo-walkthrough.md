# Demo Walkthrough

1. Open `input-task.md` and read the one-line task.
2. Open `before.md` to show the common literal implementation failure.
3. Open `expected-obligation-ledger.json`.
4. Point out that each obligation has a surface, status, and reason.
5. Show `after.md` and compare completed, maybe, not applicable, deferred, and escalated items.

Key message:

The runtime does not assume a small code edit is a small system change. It makes propagation work inspectable before implementation is treated as done.
