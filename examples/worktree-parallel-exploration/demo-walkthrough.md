# Demo Walkthrough

1. Open `branch-schedule.json` and show which branches can start.
2. Open `worktree-map.json` and show isolated worktrees for each branch.
3. Open `path-locks.json` and point out the shared type requiring parent approval.
4. Open `merge-queue.json` and show serial integration after parallel exploration.

Key message:

The runtime encourages cheap exploration while making integration explicit, ordered, and gated.
