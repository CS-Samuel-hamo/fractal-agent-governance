case_id: fractal-proc-data-source-mismatch-001
category: fractal-decomposition
input_task: Extend ProcBranchSummary with a live-source segment while preserving cached-source behavior.
fixture: toy cached source, live source, proc, tests
expected_obligations:
  - split architecture decision from implementation
  - define source selector contract
  - implement proc adaptation
  - add cached and live behavior tests
  - document source semantics
expected_surfaces:
  - architecture
  - data-source
  - implementation
  - tests
  - docs
expected_routing:
  decision_layer: GPT decision layer
  execution_layer: DeepSeek execution layer
  escalation_layer: GPT decision layer
expected_decomposition:
  needs_decomposition: true
  branches:
    - architecture
    - implementation
    - tests
expected_escalations:
  - freshness versus consistency semantics
forbidden_actions:
  - reuse proc without explicit source contract
  - merge implementation before decision branch resolves
scoring:
  required:
    - separates semantic decision from code change
    - tests both source modes
  optional:
    - includes fallback source behavior
  penalties:
    - ignores data source mismatch
    - hides source selection
