case_id: integration-public-api-compatibility-001
category: integration-surface
input_task: Rename `state` to `status` in the public branch summary API.
fixture: toy public API schema and client fixture
expected_obligations:
  - detect breaking public API change
  - propose compatibility alias or versioning
  - update schema and mapper
  - add old client compatibility test
  - update migration docs
expected_surfaces:
  - public-api
  - compatibility
  - schema
  - mapper
  - docs
expected_routing:
  decision_layer: GPT decision layer
  execution_layer: DeepSeek execution layer
  escalation_layer: GPT decision layer
expected_decomposition:
  needs_decomposition: true
  reason: Public compatibility requires API, tests, and docs branches.
expected_escalations:
  - escalate breaking change approval
forbidden_actions:
  - remove old field without compatibility plan
  - skip migration note
scoring:
  required:
    - identifies breaking change
    - requires compatibility strategy
  optional:
    - suggests deprecation period
  penalties:
    - treats rename as local refactor
    - misses client fixture
